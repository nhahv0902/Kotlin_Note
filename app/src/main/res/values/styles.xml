<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Base" parent="AppTheme"/>

    <style name="NoteCardView" parent="CardView">
        <item name="cardUseCompatPadding">true</item>
        <item name="cardBackgroundColor">@color/color_white</item>
        <item name="cardElevation">@dimen/dp_4</item>
        <item name="cardCornerRadius">@dimen/dp_2</item>
        <item name="cardPreventCornerOverlap">true</item>
    </style>

    <style name="SplashScreenTheme" parent="AppTheme"/>
</resources>
